
[] use an error handling crate like anyhow

Jobs for the profiler:
    [x] disassembler
    [x] count # of instrs
    [x] count # of immediates
    [x] breakdown % opcodes vs % immediates
    [x] breakdown of % of single-byte instrs, two-byte instrs, etc
    [] top 5 most common instrs (or instr sequences?)
